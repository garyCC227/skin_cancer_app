import React from 'react'
import { Text } from 'react-native'

const Header = () => {
    
    return <Text>Header!</Text>
}

const styles = {
    textStyle: {
        fontSize: 20
    }
}

export default Header;